source "https://rubygems.org"

# GitHub Pages uses a curated set of gems (including Jekyll) pinned via github-pages.
# Using it locally keeps your site compatible with Pages.
gem "github-pages", group: :jekyll_plugins


